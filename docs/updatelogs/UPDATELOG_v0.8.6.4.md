# 🌸 DARIA v0.8.6.4 — Harbor Lights

**Дата:** 2026-02-14  
**Версия:** 0.8.6.4  
**Тип:** Bugfix + UI polish

## Исправления

1. Морской бой: визуализация полей стала наглядной.
- видно поле соперника и поле Даши одновременно;
- клетки отображаются как квадратная сетка;
- цветами разделены: неизвестные клетки, вода, корабли, промахи и попадания.

2. Редактор файлов: исправлена работа панели `Редактировать с Дашей`.
- стабилизирован layout (панель больше не «уезжает»);
- исправлено расширение рабочей области при открытии панели;
- устранены лишние горизонтальные сдвиги/скролл в окне редактора;
- поле запроса очищается после успешного применения правок.

3. File-assist: снижены ложные отказы при нейтральных запросах.
- добавлен повторный проход с явной инструкцией разрешённых задач;
- добавлен fallback для запросов «о себе», чтобы ответ был связным и от лица Даши.

4. Проводник:
- подтверждена работа кнопки возврата в родительскую директорию (`⬅️`).

5. Списки дел (`Дела Даши`):
- выполненные однотипные задачи агрегируются в одну строку с количеством (`×N`),
  вместо длинного списка повторов.

## Техническое

- Синхронизированы версии в `main.py`, `web/app.py`, `desktop.js`, `desktop.css`, `index.html`.
- Обновлён `User-Agent` встроенного браузера до `DARIA-Browser/0.8.6.4`.

## Проверка

- `python3 -m py_compile web/app.py main.py plugins/telegram-bot/main.py core/brain.py`
- `node --check web/static/js/desktop.js`
