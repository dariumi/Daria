# 🌸 DARIA v0.8.5.1 — Quiet Pulse

**Дата:** 2026-02-13
**Версия:** 0.8.5.1
**Кодовое имя:** Quiet Pulse
**Тип:** Bugfix + UX polish

## Главные исправления

1. **Стабильность сети и уведомлений**
- Укреплён SSE для `/api/notifications/stream` и `/api/logs/stream`.
- Добавлены keep-alive/anti-buffering заголовки и безопасное завершение при разрывах соединения.
- Это уменьшает ошибки `ERR_INCOMPLETE_CHUNKED_ENCODING` и ложные обрывы потока.

2. **Telegram plugin UI: без падений при сети**
- В `tgAction` добавлена безопасная обработка сетевых ошибок.
- Убраны `Uncaught (in promise) TypeError: Failed to fetch` при недоступности сервера.
- В интерфейсе выводится понятный статус о недоступности backend.

3. **Wiki: Markdown + стиль**
- В wiki-окне добавлен markdown-рендер (заголовки, списки, code, blockquote, ссылки).
- Добавлены стили для читаемости и структуры страницы.

4. **Самосознание: переосмысленный экран**
- Полностью обновлена подача экрана «Самосознание».
- Добавлен редактор базовой self-инструкции, которая реально влияет на поведение Даши.
- Добавлены API:
  - `GET /api/self/instruction`
  - `POST /api/self/instruction`

## Технические изменения

- `core/brain.py`
  - Добавлено хранение self-инструкции в файле `~/.daria/self_instruction.md`.
  - Self-инструкция включается в системный промпт при генерации ответов.

- `web/app.py`
  - Добавлен API self-инструкции.
  - Усилены SSE endpoints и их обработка разрывов.
  - Защищён повторный запуск `attention_thread` (без краша при повторной инициализации).

- `web/static/js/desktop.js`
  - Добавлено авто-переподключение notification stream с backoff.
  - Обновлены wiki/self окна: markdown view + editor self-инструкции.

- `web/static/css/desktop.css`
  - Новые стили для markdown wiki и окна самосознания.

- `plugins/telegram-bot/templates/window.html`
  - Безопасные fetch-вызовы и UX-реакция на offline/connection errors.

## Проверка

- `python -m compileall core web plugins main.py install.py`
- `./venv/bin/python -m unittest discover -s tests -v`
- `node --check web/static/js/desktop.js`
