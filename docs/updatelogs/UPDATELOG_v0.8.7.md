# 🌸 DARIA v0.8.7 — Warm Routine

**Дата:** 2026-02-14  
**Версия:** 0.8.7  
**Тип:** Major update (behavior + UX + systems)

## Главное

1. Реалистичный режим задач Даши.
- у задач появились длительности (`duration_min`), слот времени (`scheduled_for`) и статус (`queued`/`in_progress`/`done`);
- задачи больше не закрываются мгновенно циклом;
- ночью активность ограничена (режим отдыха, если нет недавнего взаимодействия от пользователя);
- планы стали более «живыми»: музыка, браузер, видео, чтение, дневник, игровой центр.

2. Задачи теперь видимы физически в интерфейсе.
- автономные действия запускают реальные окна (плеер, браузер, wiki, файлы, игровой центр);
- добавлен `action_data.auto_open`, чтобы действия были наблюдаемы визуально, а не «скрытой магией».

3. Личный дневник исправлен.
- убраны однотипные повторяющиеся записи про «состояние»;
- дневник теперь получает более человечные заметки: мысли, чувства, рефлексию;
- запись создаётся в `files/dasha_notes/YYYY-MM-DD.md`.

4. Игровой центр.
- переименование окна/ярлыков из «Игры Даши» в «Игровой центр»;
- Морской бой: добавлена легенда цветов клеток в UI;
- Морской бой: добавлены реакции Даши на победу/проигрыш;
- Лабиринт: анти-зацикливание, приоритет непосещённых клеток, избегание шага «туда-сюда».

5. Контекст, внимание и «живость» общения.
- снижен спам attention/proactive (интервалы увеличены);
- добавлен quiet-режим, если пользователь пишет «позже/сплю/занята/отвечу позже»;
- при долгом отсутствии в активное время возможна мягкая тревожная реплика;
- proactive получил более разнообразные естественные заходы (`Приветики ...`).

6. Русский язык и чистота формулировок.
- усилено системное правило: не смешивать русский и английский в одной фразе;
- в санитайзере добавлена чистка смешанных RU/EN-токенов и случайных иностранных хвостов.

7. Календарь.
- новый backend-менеджер календаря (`calendar.json`);
- API: `/api/calendar`, `/api/calendar/add`, `/api/calendar/delete`;
- новый UI-экран «Календарь» на рабочем столе;
- календарные события учитываются в ответах про планы.

8. Telegram: голосования.
- добавлено распознавание появления poll-сообщений;
- добавлен сценарий «Даша, проголосуй ...» с осознанным выбором варианта и ответом в чат.

## Технически

- Обновление версии до `0.8.7` по ключевым файлам.
- Обновлён `User-Agent` встроенного браузера: `DARIA-Browser/0.8.7`.

## Проверки

- `python3 -m py_compile web/app.py main.py plugins/telegram-bot/main.py core/brain.py`
- `node --check web/static/js/desktop.js`
