# 🌸 DARIA v0.8.6 — Tray, Workflow, Reliability

**Дата:** 2026-02-14  
**Версия:** 0.8.6  
**Тип:** Bugfix + UX + Telegram/Web sync polish

## Что исправлено

1. **Системный трей**
- Иконка трея теперь использует тот же visual asset, что и сайт (`/static/img/logo.png`).
- В меню трея добавлены:
  - `Открыть DARIA`
  - `Открыть лог-консоль` (открывает UI с окном логов)
  - `Открыть лог-файл`
  - `Автозапуск`
  - `Выход`
- Для Linux/macOS добавлен корректный запуск `--tray` в фоновом режиме из терминала, чтобы после закрытия терминала DARIA оставалась в трее.

2. **Списки дел / API 500**
- Усилена валидация схемы `tasks.json` в `TaskManager`.
- Исправлены падения на повреждённых/старых данных.
- Маршруты `/api/tasks` и `/api/tasks/generate-dasha-day` теперь с защитой от исключений и корректным JSON-ответом без краша сервера.

3. **Текстовый редактор + мини-чат с Дашей**
- Исправлено перекрытие кнопок в режиме помощи.
- Улучшена логика открытия/закрытия панели: окно корректно расширяется вправо, панель не «съедает» рабочую область.
- При закрытии редактора состояние панели и ширина окна корректно сбрасываются.

4. **Чаты Telegram в интерфейсе**
- Telegram-чаты визуально выделены в отдельную категорию.
- В превью истории чатов отображается автор и последнее сообщение.
- Стабилизирована группировка по `source_chat_id` (один Telegram-чат не плодит новые карточки).

5. **Стикеры в веб-чате**
- Добавлена кнопка стикеров в поле ввода чата.
- Добавлен picker с эмодзи-стикерами из `/api/stickers/catalog`.
- Добавлен стиль отображения стикера как отдельного sticker-сообщения.
- В API каталога добавлена подсказка по web-стикерам.

6. **Естественность ответов**
- Исправлен кейс неестественного ответа на «как дела».
- Убрано навязывание «интенсивности настроения» из системного контекста LLM, чтобы снизить странные формулировки.

7. **Чтение книг и развитие активности**
- Добавлен тип дела `read_book` в ежедневные задачи Даши.
- Добавлена чат-команда «почитай/прочитай книгу» (чтение из `files/books`).
- Если книг нет, Даша подсказывает куда их положить.

8. **Deep-link открытия логов**
- Добавлена поддержка `/?open=logs` (и других окон) для открытия нужного окна сразу после загрузки интерфейса.

## Файлы

- `main.py`
- `web/app.py`
- `web/static/js/desktop.js`
- `web/static/css/desktop.css`
- `web/templates/index.html`
- `core/brain.py`
- `VERSION`

## Проверка

- `python -m compileall main.py web/app.py core/brain.py`
- `node --check web/static/js/desktop.js`
- `./venv/bin/python -m unittest discover -s tests -v`
