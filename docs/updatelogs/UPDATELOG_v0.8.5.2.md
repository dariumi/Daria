# 🌸 DARIA v0.8.5.2 — Day Routine

**Дата:** 2026-02-14
**Версия:** 0.8.5.2
**Тип:** Feature + UX + behavior fixes

## Что сделано

1. **Сенсоры: теперь не заглушки**
- `POST /api/senses/see` теперь принимает описание и/или файл изображения (`multipart/form-data`).
- Добавлен базовый визуальный анализ (размер, режим, палитра) с передачей в разум.
- `POST /api/senses/hear` теперь принимает текст и/или аудиофайл, пробует распознавание речи (`whisper`/`speech_recognition`, если доступны).

2. **Новая программа: Списки дел**
- Добавлено приложение `✅ Списки дел` на рабочий стол.
- Два списка:
  - дела от пользователя,
  - дела Даши.
- Ежедневный rollover: незавершённые задачи переносятся.
- Добавлен фоновой цикл «занятости» Даши в свободное время.

3. **Музыкальные интересы Даши**
- Добавлен профиль музыки и история прослушивания.
- Можно отметить трек/ссылку как «Даша послушала».
- Прослушивание локального файла в плеере также учитывается в профиле.

4. **Файловый редактор: точечные правки и новый UI**
- Убрана «nano»-стилистика, сделан светлый редактор в общем стиле.
- Добавлена боковая мини-панель «Редактирование с Дашей», открывается кнопкой-аватаром.
- Поддержка точечного редактирования выделенного фрагмента (selection-based assist).
- Добавлена работа с форматами:
  - `txt`/текстовые,
  - `csv`,
  - `docx` (если установлен `python-docx`),
  - `xlsx` (если установлен `openpyxl`).

5. **Поведение и ритм**
- Период выключения учитывается как «сон/отдых» при старте, влияет на энергию.
- Усилен анти-залип на старой теме в разуме: при явной смене темы старый контекст ослабляется.
- Улучшено согласование после proactive-сообщений в чате.

6. **Telegram bot**
- Добавлена память отношений к пользователям в группах (пер-пользовательный affinity/context hint).
- Добавлена визуальная коммуникация: эмодзи/стикеры (стикеры — при наличии `sticker_ids` в настройках плагина).

7. **Режим в трее / демончик**
- Добавлен `--tray` запуск (если установлены `pystray` и `Pillow`).
- В трее: открыть UI, переключить автозапуск, выход.
- Добавлен базовый автозапуск (Linux/Windows).

## Ключевые файлы

- `web/app.py`
- `web/static/js/desktop.js`
- `web/static/css/desktop.css`
- `web/templates/index.html`
- `core/brain.py`
- `plugins/telegram-bot/main.py`
- `main.py`

## Проверка

- `python -m compileall web/app.py plugins/telegram-bot/main.py main.py`
- `node --check web/static/js/desktop.js`
- `./venv/bin/python -m unittest discover -s tests -v`
