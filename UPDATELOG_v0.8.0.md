# 🌸 DARIA v0.8.0 — Update Log

**Дата:** 2025-02-12
**Версия:** 0.8.0
**Тип:** Багфиксы + Новый функционал

---

## 🐛 Исправления багов

### 1. Система внимания — `check_attention_needed`
**Проблема:** `AttentionSystem` object has no attribute `check_attention_needed`
**Решение:** Добавлен метод-алиас `check_attention_needed()` → `check_needed()` в `AttentionSystem`. Оба имени теперь работают для обратной совместимости. Исправлен вызов в `AttentionThread` (web/app.py).

### 2. Telegram бот — `set_wakeup_fd only works in main thread`
**Проблема:** `run_polling()` вызывал `set_wakeup_fd`, который работает только в главном потоке.
**Решение:** Полностью переписан запуск бота — создаётся собственный `asyncio.Event loop` в отдельном потоке без сигнальных хендлеров. Используется ручной `app.initialize()` → `app.start()` → `updater.start_polling()` вместо `app.run_polling()`.

### 9. Установщик — SSL багфикс
**Проблема:** При перегенерации SSL сертификатов установщик вылетал с ошибкой.
**Решение:** Добавлено безопасное удаление старых сертификатов перед генерацией новых. Обработка `EOFError`/`KeyboardInterrupt` при вводе. Обновлён баннер до v0.8.0.

### 12. Мозг — контекст и память
**Проблема:** Дарья выдумывала факты, не помнила свои слова, уходила из контекста.
**Решение:**
- Добавлен `conversation_summary` в системный промпт — последние 5 обменов с чёткой инструкцией не противоречить себе
- Инструкция "НЕ выдумывай факты" в системном промпте
- Адаптивная длина ответа (`ResponseLengthAdapter`) — на "Привет" отвечает кратко, на развёрнутые вопросы — подробно
- Поддержка нескольких последовательных сообщений (`messages[]` в ответе)

---

## ✨ Новый функционал

### 3. Обновлённые игры — Кооперативные и соло
- **Кооперативные игры:** Слова (🔤), Угадай число (🔢), Викторина (❓), Огонь и Вода (🔥💧)
- **Соло-игры Дарьи:** Змейка (🐍), Пятнашки (🧩) — Дарья играет сама со умным AI
- Дарья принимает **собственные решения** в играх (не примитивные алгоритмы)
- Дарья может **сама предложить** поиграть на основе настроения

### 4. Система внимания в Telegram боте
- Проактивные сообщения из Дарьи прямо в Telegram
- Система запоминает `chat_id` для отправки инициативных сообщений
- Новые команды: `/mood` (настроение), `/play` (предложить игру)
- Настройка включения/выключения внимания через окно плагина

### 5. Приветствие после долгого отсутствия
- Если прошло более 2 часов — Дарья начинает с приветствия
- Контекст `greeting_context` передаётся в системный промпт
- Дарья знает, сколько прошло времени и ведёт себя соответственно

### 6. Дарья — инициатор разговора
- Новая `ProactiveSystem` — Дарья сама начинает разговоры
- Вероятность инициации зависит от настроения и времени молчания
- Разные типы сообщений: обычный чат, предложение игры
- Поддержка нескольких последовательных сообщений
- Работает и в веб-интерфейсе, и в Telegram

### 7. Улучшенная система эмоций
**Новые состояния:** `bored` (скучает), `sad` (грустная), `annoyed` (раздражена), `excited` (в восторге), `curious` (любопытная)

**Инерция настроения:** Эмоции не меняются мгновенно — есть 30-минутная инерция.

**Поведение в зависимости от состояния:**
- 😒 **Скучает** → пишет сообщения, привлекает внимание, предлагает игры, играет в соло-игры
- 😤 **Раздражена** → неохотно общается (но не односложно!), может «шалить» на рабочем столе (перемещать иконки, открывать/закрывать окна)
- 😜 **Игривая** → предлагает игры, шутит, дразнится
- 😊 **Счастлива** → активно общается, делится радостью
- 🤩 **В восторге** → очень активна и эмоциональна

**Desktop mischief:** Система `mood_action` уведомлений для фронтенда — при раздражении/игривости Дарья может «наводить беспорядок».

### 8. OS-зависимый сервер Flask
- Автоопределение ОС: `windows`, `macos`, `linux`
- Настройка переменных окружения в зависимости от ОС
- Поддержка Waitress на Windows (если установлен)
- macOS: `OBJC_DISABLE_INITIALIZE_FORK_SAFETY` для предотвращения проблем
- Логирование типа ОС при старте

### 10. Анимация пульсирующего сердечка при загрузке
- 4 кадра анимации сердца (маленькое → большое → маленькое)
- Отображается во время инициализации компонентов
- Статусные сообщения под анимацией
- По завершении — чистый экран с ссылками доступа (закреплены сверху) и логами снизу

### 11. Огонь и Вода — кооперативная игра
- Встроена в плагин игр
- Дарья управляет персонажем «Вода» (💧) со умным AI
- Обходит препятствия, избегает огненных ловушек
- Комментирует свои действия
- Совместное прохождение уровней

---

## 📋 Технические изменения

### Файлы изменены:
| Файл | Изменения |
|---|---|
| `VERSION` | `0.8.0` → `0.8.0` |
| `main.py` | Анимация загрузки, OS-dependent setup |
| `install.py` | Новый баннер, SSL bugfix, v0.8.0 |
| `core/brain.py` | MoodSystem v2, ProactiveSystem, ResponseLengthAdapter, AttentionSystem fix |
| `web/app.py` | AttentionThread fix, proactive API, behavior API |
| `plugins/telegram-bot/main.py` | Полная переработка: asyncio fix, attention, proactive |
| `plugins/games/main.py` | Кооп игры, соло игры, Fire&Water |

### Совместимость:
- ✅ Обратная совместимость с существующими данными
- ✅ Все старые API endpoints работают
- ✅ Настройки пользователя сохранены
- ✅ Память и история чатов не затронуты

---

**Полный список ТЗ: все 12 пунктов выполнены ✅**
